diff --git a/src/common.h b/src/common.h
index 3bb648a..bf82411 100644
--- a/src/common.h
+++ b/src/common.h
@@ -112,6 +112,7 @@ struct idevicerestore_client_t {
 	char* device_build;
 	int build_major;
 	char* restore_boot_args;
+	char* restore_boot_args_extra;
 	char* cache_dir;
 	unsigned char* root_ticket;
 	int root_ticket_len;
diff --git a/src/idevicerestore.c b/src/idevicerestore.c
index 04070a1..b63d099 100644
--- a/src/idevicerestore.c
+++ b/src/idevicerestore.c
@@ -1640,6 +1640,9 @@ void idevicerestore_client_free(struct idevicerestore_client_t* client)
 	if (client->restore_boot_args) {
 		free(client->restore_boot_args);
 	}
+	if (client->restore_boot_args_extra) {
+		free(client->restore_boot_args_extra);
+	}
 	if (client->cache_dir) {
 		free(client->cache_dir);
 	}
@@ -1712,6 +1715,19 @@ void idevicerestore_set_cache_path(struct idevicerestore_client_t* client, const
 	}
 }
 
+void idevicerestore_set_boot_args(struct idevicerestore_client_t* client, const char* boot_args)
+{
+	if (!client)
+		return;
+	if (client->restore_boot_args_extra) {
+		free(client->restore_boot_args_extra);
+		client->restore_boot_args_extra = NULL;
+	}
+	if (boot_args) {
+		client->restore_boot_args_extra = strdup(boot_args);
+	}
+}
+
 void idevicerestore_set_progress_callback(struct idevicerestore_client_t* client, idevicerestore_progress_cb_t cbfunc, void* userdata)
 {
 	if (!client)
diff --git a/src/idevicerestore.h b/src/idevicerestore.h
index ce8686f..1ecf523 100644
--- a/src/idevicerestore.h
+++ b/src/idevicerestore.h
@@ -84,6 +84,7 @@ void idevicerestore_set_udid(struct idevicerestore_client_t* client, const char*
 void idevicerestore_set_flags(struct idevicerestore_client_t* client, int flags);
 void idevicerestore_set_ipsw(struct idevicerestore_client_t* client, const char* path);
 void idevicerestore_set_cache_path(struct idevicerestore_client_t* client, const char* path);
+void idevicerestore_set_boot_args(struct idevicerestore_client_t* client, const char* boot_args);
 void idevicerestore_set_progress_callback(struct idevicerestore_client_t* client, idevicerestore_progress_cb_t cbfunc, void* userdata);
 
 int idevicerestore_start(struct idevicerestore_client_t* client);
diff --git a/src/recovery.c b/src/recovery.c
index b843f1e..3c70e89 100644
--- a/src/recovery.c
+++ b/src/recovery.c
@@ -130,6 +130,16 @@ int recovery_enter_restore(struct idevicerestore_client_t* client, plist_t build
         client->restore_boot_args = strdup("rd=md0 nand-enable-reformat=1 -progress");
     }
 
+    /* Append caller-supplied restore boot-args (restorekit --boot-args), e.g.
+     * transport-restriction probes, to whatever default was chosen above. */
+    if (client->restore_boot_args_extra && client->restore_boot_args) {
+        size_t len = strlen(client->restore_boot_args) + strlen(client->restore_boot_args_extra) + 2;
+        char* combined = malloc(len);
+        snprintf(combined, len, "%s %s", client->restore_boot_args, client->restore_boot_args_extra);
+        free(client->restore_boot_args);
+        client->restore_boot_args = combined;
+    }
+
 	/* upload data to make device boot restore mode */
 
 	if(client->recovery == NULL) {
@@ -510,10 +520,11 @@ int recovery_send_kernelcache(struct idevicerestore_client_t* client, plist_t bu
 	irecv_usb_control_transfer(client->recovery->client, 0x21, 1, 0, 0, 0, 0, 5000);
 
 	if (client->restore_boot_args) {
-		char setba[256];
-		strcpy(setba, "setenv boot-args ");
-		strcat(setba, client->restore_boot_args);
+		size_t setba_len = strlen("setenv boot-args ") + strlen(client->restore_boot_args) + 1;
+		char* setba = malloc(setba_len);
+		snprintf(setba, setba_len, "setenv boot-args %s", client->restore_boot_args);
 		recovery_error = irecv_send_command(client->recovery->client, setba);
+		free(setba);
 	}
 
 	recovery_error = irecv_send_command_breq(client->recovery->client, "bootx", 1);
