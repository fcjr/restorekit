{{#each licenses}}
<section class="lic">
<h3 class="lic-name">{{name}}</h3>
<div class="lic-used">{{#each used_by}}{{crate.name}} {{crate.version}}{{#unless @last}} · {{/unless}}{{/each}}</div>
<pre class="lic-text">{{text}}</pre>
</section>
{{/each}}
